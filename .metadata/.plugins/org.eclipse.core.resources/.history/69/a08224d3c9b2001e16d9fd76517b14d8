server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Hotel  # Update this line with your PostgreSQL host and port
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
